#include<iostream>
#include<string>
using namespace std;
int main()
{
    long long n, m, k, ar;
    cin >> n >> m >> k;
    if(n*m%k)
        puts("No");
    else{
        ar = n * m / k;
        
    }
}